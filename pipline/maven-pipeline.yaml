apiVersion: tekton.dev/v1
kind: Pipeline
metadata:
  name: maven-pipeline
  namespace: rhn-consulting-kemor-dev
spec:
  params:
    - name: repo-url
      type: string
    - name: context-path
      type: string
    - name: SONAR_HOST_URL
      type: string
    - name: SONAR_PROJECT_KEY
      type: string
  tasks:
    - name: fetch-repo
      params:
        - name: url
          value: $(params.repo-url)
        - name: subdirectory
          value: ''
        - name: deleteExisting
          value: 'true'
      taskRef:
        kind: ClusterTask
        name: git-clone
      workspaces:
        - name: output
          workspace: shared-workspace
    - name: mvn-build
      params:
        - name: CONTEXT_DIR
          value: $(params.context-path)
        - name: GOALS
          value:
            - '-DskipTests'
            - clean
            - compile
      runAfter:
        - fetch-repo
      taskRef:
        kind: ClusterTask
        name: maven
      workspaces:
        - name: source
          workspace: shared-workspace
        - name: maven-settings
          workspace: maven-settings
    - name: mvn-tests
      params:
        - name: CONTEXT_DIR
          value: $(params.context-path)
        - name: GOALS
          value:
            - test
      runAfter:
        - mvn-build
      taskRef:
        kind: ClusterTask
        name: maven
      workspaces:
        - name: source
          workspace: shared-workspace
        - name: maven-settings
          workspace: maven-settings
    - name: mvn-package
      params:
        - name: CONTEXT_DIR
          value: $(params.context-path)
        - name: GOALS
          value:
            - package
      runAfter:
        - mvn-tests
      taskRef:
        kind: ClusterTask
        name: maven
      workspaces:
        - name: source
          workspace: shared-workspace
        - name: maven-settings
          workspace: maven-settings
    - name: sonarqube-scanner
      taskRef:
        name: sonarqube-scanner
      runAfter:
        - mvn-package
      params:
        - name: SONAR_HOST_URL
          value: $(params.SONAR_HOST_URL)
        - name: SONAR_PROJECT_KEY
          value: $(params.SONAR_PROJECT_KEY)
      workspaces:
        - name: source-dir
          workspace: shared-workspace
        - name: sonar-settings
          workspace: sonar-settings
  workspaces:
    - name: shared-workspace
    - name: maven-settings
    - name: sonar-settings
